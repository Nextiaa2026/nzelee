/** Full-page wrapper for sign-in, register, password flows — white page, no tinted canvas. */
export const authPageShellClass =
  "flex min-h-svh flex-col items-center justify-center gap-6 bg-background p-6 md:p-10";

/** Card on auth routes: solid white panel, neutral border (not theme-tinted surfaces). */
export const authCardClassName =
  "border border-neutral-200 bg-white text-neutral-900 shadow-sm ring-0";
